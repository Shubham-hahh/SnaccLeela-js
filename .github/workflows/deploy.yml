name: Deploy Full Repo with Quartz

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Build Hazelbite
        run: |
          cd "pages/Hazelbite"
          npm install
          npx quartz build

      - name: Build Project-Cannon
        run: |
          cd "pages/Project-Cannon"
          npm install
          npx quartz build

      - name: Merge Everything
        run: |
          # 1. Create a clean deployment folder
          mkdir -p _site
          
          # 2. Copy the FULL repository into _site
          # This includes your index.html and assets folder
          # We exclude technical folders and the raw Quartz source
          rsync -a . _site/ --exclude .git --exclude .github --exclude node_modules --exclude pages/Hazelbite --exclude pages/Project-Cannon
          
          # 3. Create the subfolders for your built Quartz sites
          mkdir -p _site/pages/Hazelbite
          mkdir -p _site/pages/Project-Cannon
          
          # 4. Copy the BUILT files into those folders
          cp -r pages/Hazelbite/public/* _site/pages/Hazelbite/
          cp -r pages/Project-Cannon/public/* _site/pages/Project-Cannon/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_site'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5